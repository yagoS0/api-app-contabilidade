enviar-guias/secrets/credentials.json
secrets/credentials.json
secrets/credentials.json
.env
.env.*
!.env.example
certs/

# Service Account JSON (Gmail API, GCP, etc.) — nunca commitar credenciais
*-*.iam.gserviceaccount.com.json
apps/api/*.json
!apps/api/package.json
!apps/api/package-lock.json
!apps/api/jest.config.json
!apps/api/babel.config.json
!apps/api/tsconfig*.json
**/service-account*.json
**/gcp-credentials*.json
enviodeguias-*.json
# dependencies
node_modules/
apps/*/node_modules/

# Python venvs
**/.venv/
**/__pycache__/
*.pyc

# Secrets — TODO arquivo de certificado e chave privada
**/secrets/
**/credentials*.json
**/*.pem
**/*.key
**/*.pfx
**/*.p12
**/*.crt
**/*.cer
**/*.pfx:Zone.Identifier
**/*.cer:Zone.Identifier

# Dados fiscais sensíveis (storage dinâmico — gerado em runtime,
# NUNCA deve ir pro repo). PDFs DAS/INSS, XMLs NF-e/NFS-e, certs A1
# de empresas clientes, etc.
apps/api/storage/
storage/
**/storage/certificates/
**/storage/guides/
**/storage/notas/

# XML de nota solto na RAIZ do repo. Já aconteceu: um XML de NFS-e Nacional real
# (nome do arquivo = chave de acesso de 50 dígitos, CNPJ/endereço/telefone do
# prestador e do tomador dentro) ficou versionado aqui e passou batido pela
# anonimização. A raiz não tem XML legítimo — a amostra de leiaute ANONIMIZADA
# vive em docs/leiaute-nfse/, fora do alcance desta regra.
# ⚠ Isto cobre só a raiz. Nota real largada em subpasta continua entrando.
/*.xml

# Build artifacts
apps/web/dist/
apps/api/node_modules/

# Jest coverage (gerado em test:coverage)
coverage/
apps/*/coverage/

# OS
*:Zone.Identifier
.DS_Store

# Credenciais Google
apps/api/enviodeguias-*.json
apps/api/*-service-account.json

# ⚠ LISTA DE DESFAZER da classificação (`scripts/rodar-classificacao.mjs`).
# Ela guarda MILHARES de ids de NotaItem de PRODUÇÃO para permitir reverter a execução.
# É artefato operacional, não código: entra no histórico do git para sempre e não serve a
# ninguém que clone o repo. O script a escreve no CWD; guarde-a FORA da árvore.
desfazer-classificacao-*.json
