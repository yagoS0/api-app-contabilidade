# Modo da camada de API: mock | real | real_with_mock_fallback (default: mock)
VITE_API_MODE=mock
# Base da API do contador (Express). Só é lida nos modos real/fallback.
VITE_API_BASE_URL=http://localhost:3000
