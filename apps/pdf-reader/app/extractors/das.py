from typing import Any


def refine_simples(fields: dict[str, Any], _text_upper: str) -> dict[str, Any]:
    """DAS / Simples Nacional — base regex usually sufficient; hook for future rules."""
    return fields
