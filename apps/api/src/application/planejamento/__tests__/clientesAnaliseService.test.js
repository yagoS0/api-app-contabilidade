jest.mock('../../../infrastructure/db/prisma.js',()=>({prisma:{}}));
jest.mock('../../notas/apuracao/v2/FechamentoService.js',()=>({whereFaturamentoEmit:()=>({papel:'EMIT',statusEfetivo:'autorizada'})}));
import { obterClientesAnalise } from '../ClientesAnaliseService.js';
test('histórico completo escopado e sem documentos binários',async()=>{const findMany=jest.fn(async()=>[]);await obterClientesAnalise({portalClientId:'a',de:'2026-08',ate:'2026-08',client:{portalInvoice:{findMany}}});expect(findMany).toHaveBeenCalledTimes(1);const q=findMany.mock.calls[0][0];expect(q.where).toMatchObject({clientId:'a',papel:'EMIT',statusEfetivo:'autorizada'});expect(q.where.competencia.gte).toBeUndefined();expect(q.select.xmlRaw).toBeUndefined();expect(q.select.tomadorDoc).toBe(true);});
test('limite nunca apresenta total truncado',async()=>{await expect(obterClientesAnalise({portalClientId:'a',de:'2026-08',ate:'2026-08',client:{portalInvoice:{findMany:async()=>new Array(20001)}}})).rejects.toThrow('HISTORICO_EXTENSO');});
