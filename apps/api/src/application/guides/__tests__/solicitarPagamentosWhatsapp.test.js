jest.mock('../../fiscal/serpro/CompanyRotinasService.js',()=>({idsComRotinaAtiva:jest.fn()}));
import {solicitarPagamentosWhatsapp} from '../SolicitarPagamentosWhatsappService.js';
test('varre guias sem filtrar origem ou tributo, somente empresas habilitadas',async()=>{const db={guide:{findMany:jest.fn(async()=>[{id:'fgts',vencimento:'2026-09-01'},{id:'iss',vencimento:'2026-09-02'},{id:'futura',vencimento:'2026-10-20'}])}};const avisar=jest.fn(async()=>({status:'ENVIADO'}));await solicitarPagamentosWhatsapp({scheduledAt:'2026-09-25',agora:new Date('2026-09-25T12:00Z')},{db,idsAtivos:async()=>['c'],avisar});const where=db.guide.findMany.mock.calls[0][0].where;expect(where).toMatchObject({portalClientId:{in:['c']},liberadaCliente:true,baixada:false});expect(where.source).toBeUndefined();expect(where.tipo).toBeUndefined();expect(avisar.mock.calls.map(c=>c[0].guideId)).toEqual(['fgts','iss']);});
test('execução manual não dispara solicitação automática',async()=>{const db={guide:{findMany:jest.fn()}};expect(await solicitarPagamentosWhatsapp({}, {db})).toEqual({total:0,resultados:[]});expect(db.guide.findMany).not.toHaveBeenCalled();});
