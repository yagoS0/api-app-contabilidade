import { render, screen, waitFor } from '@testing-library/react';
import { FluxoLeitura } from '../FluxoLeitura';
import { linhaDoMes, linhasDosDias } from '../../lib/tabelaFluxoLeitura';
import { linhaDoMes as doCliente, linhasDosDias as diasDoCliente } from '../../../../../../portal-cliente-web/src/features/painel/lib/tabelaDoFluxo';
const mes={competencia:'2026-08',linhas:[{dia:1,direcao:'ENTRADA',fonte:'NOTA_EMITIDA',procedencia:'FATO',valor:100},{dia:5,direcao:'SAIDA',fonte:'GUIA',procedencia:'COMPROMISSO',valor:20},{dia:null,direcao:'SAIDA',fonte:'FOLHA',procedencia:'PREVISAO',valor:10}]};
test('agregação mantém paridade com cliente, inclusive acumulado e sem dia',()=>{expect(linhaDoMes(mes)).toEqual(doCliente(mes));expect(linhasDosDias(mes,31)).toEqual(diasDoCliente(mes,31));});
test('renderiza todos os dias com rolagem e nenhum botão operacional',async()=>{const api={getFluxoCaixa:jest.fn(async()=>({ok:true,demonstracao:false,meses:[mes,{competencia:'2026-09',linhas:[]}],folha:{disponivel:true}}))};const {container}=render(<FluxoLeitura api={api} companyId="a" competenciaReferencia="2026-08" razaoSocial="Empresa A"/>);await screen.findByRole('region',{name:/Fluxo diário de agosto/});expect(container.querySelectorAll('tbody tr')).toHaveLength(61);expect(screen.queryAllByRole('button')).toHaveLength(0);expect(api.getFluxoCaixa).toHaveBeenCalledWith('a',{janelaInicio:'2026-08'});});
test('falha ao trocar empresa remove tabela antiga',async()=>{const api={getFluxoCaixa:jest.fn().mockResolvedValueOnce({ok:true,demonstracao:false,meses:[mes]}).mockRejectedValueOnce(new Error('Sem acesso'))};const {rerender}=render(<FluxoLeitura api={api} companyId="a" competenciaReferencia="2026-08"/>);await screen.findByRole('region');rerender(<FluxoLeitura api={api} companyId="b" competenciaReferencia="2026-08"/>);await screen.findByRole('alert');await waitFor(()=>expect(screen.queryByRole('table')).not.toBeInTheDocument());});
